# Test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# Then run the suite from backend/ with:  python -m pytest
pytest>=8,<10
httpx>=0.27          # required by fastapi.testclient
