"""Resume parsing service (stub).

Extracts a simple profile (skills, education, experience summary, target roles)
from raw resume text. Implemented in a later slice. No LLM calls yet.
"""


def parse_resume(raw_resume: str) -> dict:
    """Return a simple structured profile dict from resume text."""
    raise NotImplementedError("resume_parser.parse_resume not implemented yet")
